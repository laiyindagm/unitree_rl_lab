# Unitree G1 15DOF 低速死区问题：实验演变与解决方案报告

> 项目：unitree_rl_lab — G1 15自由度人形机器人旋转/低速行走训练
> 算法：PPO / LCP-PPO，IsaacLab 仿真，rsl_rl 框架
> 时间跨度：2025年4月
> 版本线索：V2 → V3 → V4 → V5 → V6 → V7 → V8 → V9 → V10 → V11 → V12

---

## 目录

1. [项目背景与目标](#1-项目背景与目标)
2. [核心困境：低速死区问题](#2-核心困境低速死区问题)
3. [实验演变历程](#3-实验演变历程)
4. [关键经验与教训](#4-关键经验与教训)
5. [当前结论与方案](#5-当前结论与方案)
6. [未来展望](#6-未来展望)

---

## 1. 项目背景与目标

训练 Unitree G1 15DOF 人形机器人在仿真（IsaacLab / IsaacSim）中学习**全向行走 + 原地旋转**能力，并通过 sim2sim（PhysX → MuJoCo）验证策略鲁棒性，最终部署至实机。

**训练目标**：
- 能跟踪任意线速度指令（前后左右，0 ~ 1.5 m/s）
- 能跟踪任意角速度指令（原地旋转，±0.8 rad/s）
- 零指令时静止站立，状态切换平滑
- 上半身（腰部关节）稳定，不晃动

**核心困境**：策略在低速（< 0.3~0.5）区间"理性地不动"——收到小速度指令时选择站立不动，因为**站着不动的奖励反而比尝试跟踪更高**。

---

## 2. 核心困境：低速死区问题

### 2.1 数学根因

速度跟踪奖励采用 Gaussian 核：

    r_track = exp(-error² / σ²),    σ = 0.5

当指令 v_cmd = 0.3，策略若选择**站立不动**（v_actual = 0）：

    r_standing = exp(-0.3² / 0.5²) = exp(-0.36) ≈ 0.698

若**完美跟踪**（v_actual = v_cmd）：

    r_perfect = 1.0

因此，尝试跟踪的**边际收益**为：

    Δr = 1.0 - 0.698 = 0.302

而 action_rate_l2 惩罚（权重 -0.12）在运动时的典型代价约为 **0.327/step**。

    边际收益(0.302) < 行动代价(0.327)

**策略合理地选择站立不动** —— 这不是训练 bug，而是奖励函数设计的数学必然。

### 2.2 死区范围

| 速度类型 | 死区阈值 | 说明 |
|---------|---------|------|
| 线速度 (lin_vel) | ~0.4 m/s | cmd < 0.4 时站立收益 > 跟踪收益 |
| 角速度 (ang_vel) | ~0.3–0.5 rad/s | 同理，原地旋转指令 < 0.5 无响应 |

### 2.3 死区的影响

- **低速行走不可用**：指令 0.1~0.3 m/s 完全无响应
- **原地旋转受限**：角速度 < 0.5 无法触发，影响灵活性
- **静止→运动切换突兀**：没有平滑过渡，突然从 0 跳到 0.5
- **课程学习受阻**：分桶课程中低速桶"看起来已经满分"（因为站着不动 reward ≈ 0.7），影响解锁门槛

### 2.4 两个修复方向

| 方向 | 原理 | 方案 | 问题 |
|-----|------|------|------|
| **增大边际收益** | 缩小 σ → 站着不动的"免费奖励"降低 | 自适应 σ / σ 退火 | 过早缩 σ 破坏早期训练 |
| **降低行动代价** | 低速时减小 action_rate 惩罚 | 速度缩放 action_rate | 单独使用效果有限 |

---

## 3. 实验演变历程

### 3.1 V2—V3：起步与 LCP 探索

**V2（LCP 初次引入）**

三个 V2 实验（V2a LCP、V2b BasePPO、纯 LCP 替换）全部因 NaN 崩溃。

- **根因**：没有观测裁剪、动作裁剪、关节速度终止条件
- **崩溃路径**：极端观测值 → NaN → log_std_param=NaN → exp(NaN) → 采样崩溃
- **关联**：bad_orientation 越高 → 崩溃越早（V2a: 96%→2.7h, V2b: 30%→5.5h）

**修复措施**：
- ObsTermCfg.clip=(-5, 5) 对关节/动作观测裁剪
- RslRlOnPolicyRunnerCfg.clip_actions=100.0
- joint_vel_out_of_manual_limit 终止条件（max_velocity=100）

**V3（安全化 + LCP 结论）**

| 版本 | 特点 | bad_orient | 结论 |
|-----|------|-----------|------|
| V3a | LCP + 硬化最小化 | 20.29% | LCP 冻结 std=0.5 妨碍旋转 |
| V3b | BasePPO + 安全探索 | 0.69% | 行为OK，但完全不旋转 |
| V3c | LCP + 平面渐进 | 94.03% | 灾难性失败 |
| V3d | V3c + 对称增强 | — | — |

**结论**：LCP 显著降低奖励（冻结 std=0.5 太保守）。**放弃 LCP，改用 BasePPO**。

V3b 虽 bad_orient 低但不旋转的根因：缺少 rotation_double_support_slide 和 rotation_twist_joint 奖励项 → 策略找到"踏步不旋转"的稳定局部最优。

---

### 3.2 V4：对称性增强的失败

基于 V2b 的 27 个奖励项 + 观测裁剪 + 物理对称性数据增强。

| 版本 | reward | bad_orient | 对比基线(≈50, 0.69%) |
|-----|--------|-----------|---------------------|
| V4a | 10.64  | 65%       | 远差于基线 |
| V4b | 19.91  | 38%       | 差于基线 |
| V4c | 22.21  | 30%       | 差于基线 |

**失败原因**：

1. **rsl_rl 数据增强实现有 bug**：old_actions_log_prob.repeat(num_aug,1) 对镜像观测复用原始 log_prob → 重要性采样比率偏差
2. **对称性收敛后增强退化**：symmetry_loss→0 时退化为无用批次复制
3. **V2b 本身的问题被继承**：V2b 弱化旋转信号 3×，强化惩罚 4×，减少旋转练习（20%→5%）

**教训**：对称性增强的实现必须在 rollout 时独立采样（非 PPO 更新时复制），且需要显式对称性损失梯度。

---

### 3.3 V5：回归基线与 DR 胜出

**核心原则**：旋转基线（rot base，reward≈50, ep_len=1000）可用。所有改动必须**最小增量**。

| 版本 | 特点 | sim2sim 表现 |
|-----|------|-------------|
| V5a | rot + 4 行为奖励 + action_rate 调优 | 旋转 OK，上半身不稳 |
| V5b | V5a + 镜像损失（非数据增强） | 比 V5a 略好，镜像损失未收敛 |
| V5c | V5a + **域随机化**(DR) + action_rate -0.10 | **最佳** — 行走/稳定/跟踪均好 |

V5c 的 DR 配置：执行器增益 ±20%，质心偏移 ±5cm，外力 ±10N/±3Nm，推击 ±0.8m/s（5-8s 间隔）

**结论**：**DR 显著提升 sim2sim 鲁棒性**，V5c 成为后续所有实验的基础平台。

---

### 3.4 V6：稳定性加固与死区浮现

基于 V5c 解决：(1) 上半身振荡，(2) 指令切换平滑度，(3) 静止→旋转，(4) 后倾。

| 版本 | 特点 | ang_vel_cmd | sim2sim |
|-----|------|------------|---------|
| V6a | 激进稳定性 | 0.3 | 无法启动旋转 |
| V6b | V6a + 旋转增强 | 0.4 | 静止振荡随时间恶化 |
| V6c | V6b + 更强 DR + 步态调优 | **0.7** | **旋转课程最佳！** |

V6c 最终配置亮点：
- 步态周期 0.8→0.7s
- feet_slide -0.3，action_rate -0.12
- 更强 DR（外力 ±15N，摩擦 0.2-1.0，推击 ±1.0m/s）
- 角速度课程达到 0.7（历史最高）

**死区正式确认**：V6b sim2sim 中 < 0.3 的线速度和角速度完全无响应。首次从数学角度推导出死区根因（见第 2 节）。

**V6c 成为后续所有实验的基础配置。**

---

### 3.5 V7：暴力惩罚的灾难

设计思路：用新的奖励函数直接"惩罚"死区行为。

新增 4 个奖励函数：
- velocity_mismatch_l1（权重 -1.0）：L1 速度误差惩罚
- standstill_joint_vel（权重 -0.5）：静止时关节速度惩罚
- waist_joint_vel_penalty（权重 -0.3）：腰部关节速度惩罚
- action_rate_scaled_by_vel：速度自适应 action_rate

**结果：灾难性失败**

| 版本 | bad_orient | ep_len | reward |
|-----|-----------|--------|--------|
| V7a | 100% | ~50 | 0.26 |
| V7b | 100% | ~60 | 0.85 |
| V7c | 100% | ~63 | 1.57 |

**根因分析**：

惩罚权重 **~10× 太大**：
- velocity_mismatch(-1.0) → -0.037/step
- standstill_joint_vel(-0.5) → -0.033/step
- waist_joint_vel(-0.3) → -0.024/step
- **总新增惩罚**：-0.094/step
- **存活奖励**：仅 +0.009/step

策略学到**"故意摔倒"比站着承受惩罚更优**（死亡终止惩罚积累）。

> **教训**：新增惩罚应 ≤ 总惩罚预算的 10%。必须在设计阶段先计算每步贡献值。

---

### 3.6 V8：三路并进与自适应 σ 死锁

三种不同范式的修复尝试：

**V8a "温和低速修复"**：V6c + 权重缩小 10× 的新惩罚
- 结果：低速旋转仍失败（< 0.3），前进步态不协调，但能跑
- **waist_joint_vel(-0.04) 确认有效**（腰部晃动改善）

**V8b "自适应 σ 跟踪"**（首次尝试从数学根源修复死区）：
- 将固定 σ=0.5 替换为 σ = max(σ_min, scale × |v_cmd|)
- σ_min=0.15：在 cmd=0.2 时，站着不动奖励从 0.85 降到 0.17 → 边际收益 0.83
- **结果：课程死锁**。lin_vel_cmd 卡在 0.1，永远无法扩展
- **根因**：自适应 σ 让早期训练太难 → 策略无法学会基本站立 → 课程无法推进
- sim2sim：灾难性表现 — 大幅晃动，无旋转，任何速度都步态混乱

**V8c "低速课程"**：调整指令分布
- rel_standing 0.05→0.15（更多站立练习），rel_rotating 0.30→0.25
- 结果：类似 V8a，但**上半身晃动改善**（waist_joint_vel -0.04 生效）
- **但** ang_vel_cmd 从 V6c 的 0.7 降到 0.3 — 减少旋转比例损害旋转课程

> **教训**：自适应 σ（per-sample）导致课程死锁。waist_joint_vel 是 V8 唯一有效输出。旋转比例 rel_rotating 不可低于 30%。

---

### 3.7 V9：分桶课程与旋转突破

三个正交方向：

**V9a "步态信号修复"**（观测层）：
- 添加 gait_phase_speed_adaptive（sin/cos 2D）到观测 → 54→56D/帧
- walk_period=1.0, run_period=0.7, 静止衰减=0.95
- 结果：**旋转无响应**（> 0.5）。根因：walk_period=1.0（观测）与步态奖励 period=0.7（V6c）冲突

**V9b "动作惩罚重塑"**（奖励层）：
- action_rate_scaled_by_vel(-0.15) + velocity_mismatch_l1(-0.20)
- 结果：死区持续（0.1-0.2），腰部更差（±0.07）
- **结论**：单独的惩罚重塑不足以打破死区

**V9c "分桶速度课程"**（课程层）⭐：
- 替换原有线性课程 → 分桶：Bucket 0 (<0.3), Bucket 1 (0.3-0.6), Bucket 2 (>0.6)
- 解锁门槛 0.6，每桶 50 样本
- **结果**：
  - **旋转响应 > 0.1（历史最佳！）**，静止→旋转平滑过渡
  - 但：**课程卡在 Bucket 0**，lin_vel 范围永远没有扩展 → 无法前进行走
  - 8000 weights 的 sim2sim 转圈能力最强

**V9c 课程 Bug 根因**：
- speed_bucketed_vel_curriculum 使用了**累积平均**（running average，从 step 0 开始）
- 早期训练策略差（reward ≈ 0.1-0.3），贡献了大量低质量样本
- 累积平均 ≈ 0.45 < 解锁门槛 0.6 → 永远卡在 Bucket 0
- 对比：lin_vel_cmd_levels（正常工作）使用**快照评估**（只看当前批次）

---

### 3.8 V10：课程修复与上半身稳定

修复 V9c 的课程 Bug 后 + init_at_random_ep_len 兼容修复后的重测试。

**关键修复**：
- 分桶课程改为快照评估 + 缓冲累积方案（兼容 init_at_random_ep_len 的异步 reset）
- init_at_random_ep_len=True 使每步只有 ~4 个环境 reset（4096/1000），需要缓冲累积足够样本

| 版本 | 特点 | sim2sim | 结论 |
|-----|------|---------|------|
| V10a | 修复后分桶 + waist(-0.08) | lin>0.4, ang>0.5 | 课程正常运行（Bucket 2） |
| V10b | V10a + 步态时钟 | 无法原地旋转 | **步态时钟有害** |
| V10c | 迭代式课程(2k/5k/8k) | 类似 V10a | ang>0.5, waist 0.06 |
| V10d | 分桶 + **逐关节腰部惩罚** | 上半身 0.025 振幅 | **腰部稳定最佳** |

V10d 逐关节腰部惩罚配置：roll:-0.20 / pitch:-0.06 / yaw:-0.02（比统一惩罚更精确）

**结论**：
- 分桶课程正常工作但**不能修复死区**（死区是奖励数学问题，非课程问题）
- 逐关节腰部惩罚有效
- **步态时钟对旋转有害**（限制了旋转自由度）

---

### 3.9 V11：死区正面攻坚

基于 V10d（分桶课程 + 逐关节腰部 + V6c DR），正面攻击死区问题。

**V11a "自适应 σ"**（从奖励侧修复）：
- σ = max(0.15, 0.5 × |v_cmd|)
- **结果：彻底失败** — iter 5195 时 bad_orient=55.73%

失败机制：
- 固定 σ=0.5 给站着不动的"免费"跟踪奖励 = 0.96
- 自适应 σ=0.15 给站着不动的奖励 = 0.64 → **33% 奖励损失**
- 策略在早期训练中连**站立**都学不会 → bad_orient 永远高于 50%

与正常训练对比（bad_orient 收敛曲线）：

    V6c:  iter 500=69%, 1000=23%, 2000=5%, 3000=2.2%     正常
    V10a: iter 500=95%, 1000=39%, 2000=5.5%, 3000=5%     正常
    V10d: iter 500=92%, 1000=36%, 2000=7.9%, 3000=6.5%   正常
    V11a: iter 3000=60.7%, iter 5195=55.7%               永不收敛

> **核心教训**：**永远不要在早期训练中减少"简单奖励"（站立跟踪）**。自适应 σ（per-sample）与 PPO 早期训练根本不兼容。

**V11b "速度缩放 action_rate"**（从惩罚侧修复）：
- action_rate_scaled_by_vel(-0.15, min_scale=0.3) 替代 action_rate_l2(-0.12)
- 低速时惩罚缩放到 30% → 行动代价降低
- 结果：训练成功，但 **旋转阈值 > 0.5**（未改善）

**V11d "低速奖励加成"**：
- low_speed_tracking(0.4, threshold=0.5) + low_speed_rotation(0.3, threshold=0.5)
- 结果：训练成功，但奖励加成几乎未激活（0.055/0.025）

**V11e "V11b + V11d"**：
- 结果：与 V11d 几乎相同，无增量

**V11b/d/e 的旋转"退化"分析**：

所有三个版本的旋转阈值 (> 0.5) 与 V9c (> 0.1) 相比看起来是退步，但实际上：
- V9c 的旋转突破是**假象**：课程 Bug 导致永远停在 Bucket 0 → 只训练 ±0.1 线速度 → 策略"被迫"专精旋转
- V10a/V10d（课程修复后）的旋转阈值也是 ~0.5
- V11b/d/e 不是退步，而是**正常训练下的真实水平**
- 角速度死区 (~0.3-0.5) 与线速度死区是**同一个数学问题**

---

### 3.10 V12：σ 退火方案（当前）

**核心洞察**：V11a 失败的根因是 **per-sample 自适应 σ** — 同一 batch 中不同样本的 σ 不同 → 奖励尺度不一致 → 策略困惑（value function 无法学到一致的 reward landscape）。

**解决方案**：Scheduled σ Annealing（全局逐迭代 σ 退火）
- 所有样本在同一迭代共享相同的 σ → 奖励尺度一致
- σ 随训练进度缓慢缩小 → 早期训练不受影响

**三阶段设计**：

    Phase 1 (iter 0-3000):     σ = σ_start (0.5)     与 V6c/V10d 基线完全一致 → 早期安全
    Phase 2 (iter 3000-8000):  σ 线性退火              策略已稳定，开始收紧
    Phase 3 (iter 8000+):      σ = σ_end (固定)        死区被打破

**实验矩阵**：

| 版本 | σ 退火 | 缩放 action_rate | 低速奖励加成 | 理论死区阈值 |
|-----|--------|----------------|------------|------------|
| **V12a** | lin:0.5→0.3, ang:0.5→0.25 | × | × | ~0.2 |
| **V12b** | 同 V12a | -0.15, min_scale=0.3 | × | ~0.11 |
| **V12c** | 同 V12a | -0.15, min_scale=0.3 | 0.4/0.3, threshold=0.5 | ~0.11 |

层层递进，可归因：V12a (σ退火) → +scaled_AR (V12b) → +low_speed_bonus (V12c)

**V12b 数学验证**：

Phase 3（σ_lin=0.3, σ_ang=0.25，scaled action_rate min_scale=0.3）：

cmd = 0.15 时：

    r_standing = exp(-0.15² / 0.3²) = exp(-0.25) = 0.779
    Δr = 1.0 - 0.779 = 0.221
    行动代价 = 0.15 × max(0.3, 0.15/1.0) × 0.327 = 0.15 × 0.3 × 0.327 ≈ 0.015

0.221 >> 0.015 → 理论死区阈值降至 ~0.11

**V12c 设计逻辑**：

V11d 中低速奖励加成几乎未激活（0.055/0.025），假设原因是：
1. 死区存在 → 策略不尝试低速跟踪 → accuracy ≈ 0 → bonus ≈ 0
2. V12b 打破死区后 → 策略开始尝试 → accuracy > 0 → bonus 终于提供梯度
3. V12c 验证："低速 bonus 在死区消除后是否有增量效果？"

**实现细节**：
- 新增两个奖励函数：track_lin_vel_xy_scheduled_sigma() / track_ang_vel_z_scheduled_sigma()
- 使用 env.common_step_counter // steps_per_iter 计算当前迭代
- σ 在 [anneal_start, anneal_end] 迭代区间线性插值
- 所有实现已完成、编译通过、注册完毕

---

## 4. 关键经验与教训

### 4.1 奖励设计原则

| 编号 | 教训 | 来源 |
|-----|------|------|
| **L1** | **永远不要减少"简单奖励"** — 站着不动的跟踪奖励是早期训练的关键信号，削弱它会导致策略连站立都学不会 | V8b, V11a |
| **L2** | **同 batch 奖励尺度必须一致** — per-sample 自适应（如自适应 σ）会让 value function 无法学到一致的 reward landscape | V8b, V11a |
| **L3** | **新惩罚 ≤ 总惩罚预算 10%** — 否则策略会学到"故意摔倒"来终止惩罚积累 | V7 |
| **L4** | **改一处，不改二处** — 不要同时调多个互相关联的参数 | V7 |
| **L5** | **Gaussian 跟踪核有内在死区** — σ=0.5 对低速指令给出高"免费"奖励，是数学结构问题 | V6 分析 |

### 4.2 课程学习原则

| 编号 | 教训 | 来源 |
|-----|------|------|
| **C1** | **快照评估 > 累积平均** — running average 被早期低质量数据淹没，永远达不到解锁门槛 | V9c Bug |
| **C2** | **兼容异步 reset** — init_at_random_ep_len 使每步 reset 数极少，需缓冲累积 | V10 修复 |
| **C3** | **旋转比例 ≥ 30%** — rel_rotating 从 30% 降到 25% 直接导致角速度课程退步 | V8c |
| **C4** | **课程 Bug 可制造虚假能力** — V9c 卡在 Bucket 0 反而让 sim2sim 旋转最好，但那是假象 | V9c→V11 |

### 4.3 架构与训练原则

| 编号 | 教训 | 来源 |
|-----|------|------|
| **A1** | **DR 是 sim2sim 的王道** — V5c（有 DR）远优于 V5a/V5b（无 DR） | V5 |
| **A2** | **LCP 冻结 std 对旋转任务太保守** — BasePPO 可学习 std 更适合高维行为空间 | V3 |
| **A3** | **对称性数据增强在 rsl_rl 中有实现 bias** — 重要性采样比率偏差 | V4 |
| **A4** | **步态时钟对原地旋转有害** — 时钟信号限制了旋转自由度 | V10b |
| **A5** | **逐关节腰部惩罚 > 统一惩罚** — roll/pitch/yaw 权重分离更精确 | V10d |
| **A6** | **基线可用就从基线出发** — V5 回归基线的做法远优于 V4 的大幅重构 | V4→V5 |

### 4.4 工程与调试原则

| 编号 | 教训 | 来源 |
|-----|------|------|
| **E1** | **设计前先计算每步贡献值** — 在 Python 中验算，不要靠直觉猜权重 | V7 |
| **E2** | **Sim2sim 抖动可能是 sim gap 而非策略问题** — PhysX vs MuJoCo 差异需要 EMA 平滑 + DR | sim2sim 诊断 |
| **E3** | **观测维度变化需新训练** — 不能从旧 checkpoint 恢复（网络结构不匹配） | V9a |
| **E4** | **TensorBoard 曲线会骗人** — track_lin_vel 高不代表低速跟踪好（站着不动也能得 0.7） | V6-V11 |

---

## 5. 当前结论与方案

### 5.1 当前状态

- **V10d** 是当前最佳基线：课程正常（Bucket 2）、上半身稳定（0.025 振幅）、DR 鲁棒
- **死区未解决**：lin_vel < 0.4、ang_vel < 0.5 无响应（所有正常训练版本一致）
- **V12a/b/c 已实现**，等待训练验证

### 5.2 V12 方案预期

| 如果 V12a 成功 | 说明 σ 退火单独有效 |
|--|--|
| 如果 V12b 优于 V12a | 说明两侧夹攻有增量收益 |
| 如果 V12c 优于 V12b | 说明低速 bonus 在死区消除后确实有用 |
| 如果 V12a 失败且 V12b 成功 | 说明缩放 action_rate 是关键 |
| 如果全部失败 | 需更激进方案（见第 6 节） |

### 5.3 已被排除的方案

| 方案 | 原因 | 版本 |
|-----|------|------|
| LCP | 冻结 std 太保守 | V3 |
| 数据增强对称性 | rsl_rl 实现有 bias | V4 |
| 暴力增加惩罚 | 导致故意摔倒 | V7 |
| Per-sample 自适应 σ | 破坏早期训练 + value 混乱 | V8b, V11a |
| 步态时钟 | 对旋转有害 | V10b |
| 减少旋转比例 | 损害旋转课程 | V8c |
| 动态奖励权重 | 同 V11a 的 value 混乱问题 | 分析 |

---

## 6. 未来展望

### 6.1 V13：动态指令分布（HEM）

如果 V12 成功打破死区，可引入 Hard Example Mining (HEM) 对指令空间进行动态采样：

- 统计各速度区间的跟踪误差
- 误差高的区间获得更多采样概率
- 实现：每 N 次迭代更新采样权重，平滑变化避免 PPO 分布偏移
- 前提：必须先解决死区，否则 HEM 会把低速区间当"hard example"疯狂采样，但策略数学上无法提升

### 6.2 架构升级（长期）

- **RNN/LSTM Encoder**：rsl_rl 原生支持 recurrent.actor/critic=True。LSTM 比 5-frame MLP 能更好建模时序信息（平衡、步态、命令过渡）
- **Transformer History Model**：已有 TransformerHistoryModel 实现可参考
- **Encoder-Actor 分离蒸馏**：参考 FEAP 的 Encoder(LSTM h=256) + Actor(LSTM h=64) 双模型架构
- **无偏对称性**：修复 rsl_rl 的对称性实现（在 rollout 时增强，非 PPO 更新时复制）

### 6.3 Sim2Real 准备

- 外力/推击 DR 已在 V6c+ 各版本中启用
- 执行器增益 DR（0.75-1.25）和摩擦 DR（0.2-1.0）已启用
- EMA 动作平滑（alpha=0.2）已在 C++ 控制器中实现
- 部署代码（g1_15dof_keyboard, g1_29dof 等）已就绪

---

## 附录 A：实验总览表

| 版本 | reward | ep_len | bad_orient | ang_vel_cmd | sim2sim 关键表现 | 状态 |
|-----|--------|--------|-----------|------------|----------------|------|
| V2a-c | — | — | — | — | NaN 崩溃 | 失败 |
| V3a | 30.25 | 856 | 20.3% | — | — | LCP 太弱 |
| V3b | 50.41 | 1000 | 0.69% | 0.2 | 不旋转 | 基线 |
| V4a-c | 10-22 | 267-564 | 30-65% | — | 全面退步 | 失败 |
| V5a | 51.42 | 988 | 0.44% | 0.6 | 旋转OK，不稳 | — |
| V5b | 52.09 | 1000 | 0.34% | 0.4 | 略好，不对称 | — |
| **V5c** | 48.36 | 1000 | 0.49% | 0.6 | **最佳 sim2sim** | DR 胜出 |
| V6a | 45.95 | 992 | — | 0.3 | 不能旋转 | — |
| V6b | 50.03 | 983 | — | 0.4 | 振荡 + 死区 | — |
| **V6c** | 44.52 | 993 | — | **0.7** | 旋转课程最佳 | 基础配置 |
| V7a-c | 0.26-1.57 | 50-63 | 100% | — | 灾难 | 失败 |
| V8a | 38.69 | 996 | 1.73% | 0.4 | 低速失败 | — |
| V8b | 32.73 | 999 | 0.79% | 0.1 | 课程死锁 | 失败 |
| V8c | 39.08 | 986 | 2.53% | 0.3 | waist 改善 | — |
| V9a | 43.10 | 977 | 2.21% | 0.3 | 旋转无响应 | — |
| V9b | 38.48 | 999 | 2.83% | 0.3 | 死区持续 | — |
| **V9c** | 43.28 | 987 | 0.74% | 0.0* | **旋转>0.1(最佳)** | 课程Bug |
| V10a | — | — | — | — | lin>0.4, ang>0.5 | — |
| V10b | — | — | — | — | 旋转失败 | 失败 |
| **V10d** | — | — | — | — | 上半身 0.025 | 当前基线 |
| V11a | 16.43 | 636 | 55.7% | — | 彻底失败 | 失败 |
| V11b | 40.40 | — | 1.6% | — | ang>0.5, lin>0.3 | — |
| V11d | 40.87 | — | 1.62% | — | bonus 未激活 | — |
| V11e | 41.30 | — | 1.17% | — | ≈ V11d | — |
| V12a | — | — | — | — | *等待训练* | 待验证 |
| V12b | — | — | — | — | *等待训练* | 待验证 |
| V12c | — | — | — | — | *等待训练* | 待验证 |

*V9c ang_vel_cmd=0.0 是因为课程卡在 Bucket 0，实际 sim2sim 旋转能力最佳

---

## 附录 B：关键文件路径

| 文件 | 说明 |
|-----|------|
| source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/rewards.py | 所有奖励函数 |
| source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/curriculums.py | 课程学习函数 |
| source/unitree_rl_lab/unitree_rl_lab/tasks/locomotion/mdp/observations.py | 观测函数 |
| source/unitree_rl_lab/unitree_rl_lab/envs/g1/15dof_rot/*.py | 所有旋转实验配置 |
| scripts/rsl_rl/train.py | 训练入口 |
| deploy/robots/g1_15dof_keyboard/ | 部署控制器代码 |

---

*报告生成于 2025 年 4 月。V12 实验结果待补充。*
